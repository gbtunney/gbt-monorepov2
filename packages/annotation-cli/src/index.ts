/**
 * `@snailicid3/annotation-cli`
 *
 * CLI for normalising Google Video Intelligence output into the `@snailicid3/annotation-core` model. Importing this
 * module has no side effects — the executable entry is `bin/annotation-cli.mjs`, which calls `main`.
 */

export * from './app.ts'
export * from './normalize.ts'
